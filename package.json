{
  "name": "chat-app",
  "private": true,
  "version": "1.0.0",
  "description": "This is an official starter Turborepo.",
  "scripts": {
    "build": "npm install --prefix apps/backend && npm install --prefix apps/web && npm run build --prefix apps/backend && npm run build --prefix apps/web",
    "dev": "npm run dev --prefix apps/backend && npm run dev --prefix apps/web",
    "start": "npm run start --prefix apps/backend",
    "kill": "taskkill /F /IM node.exe"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
